# Инспектор ИИ — сервис автоматической сверки проектной, рабочей и исполнительной документации

Прототип государственной экспертной системы Мосгосстройнадзора для автоматической сверки трёх стадий документации по 132 контролируемым параметрам. Реализован как одностраничное React-приложение на Vite с клиентским стейт-роутингом.

Стек: **React 19 + TypeScript + Vite + Tailwind CSS v4 + lucide-react + pnpm**.

---

## Быстрый старт

Требуется Node.js ≥ 20 и pnpm ≥ 9.

```bash
pnpm install
pnpm dev
```

Приложение поднимется на `http://localhost:5173` (порт Vite по умолчанию).

Сборка production-версии:

```bash
pnpm build
pnpm preview
```

Форматирование исходников:

```bash
pnpm format
```

---

## Сквозной сценарий демонстрации

Кликабельный путь без ветвлений — от входа до финализации протокола.

| Шаг | Экран | Что происходит | Куда ведёт |
|-----|-------|----------------|------------|
| 0 | **Э1 Вход** | Логин + пароль, кнопка «Войти» | Дашборд |
| 1 | **Э2 Дашборд объектов** | Сводка, фильтры, таблица 8 объектов. Клик по строке «Торговое здание, Алтуфьевское ш., 79Б» | Карточка объекта |
| 2 | **Э3 Объект и загрузка** | Три Dropzone (ПД 12, РД 5 из 15, ИД пусто). Кнопка «Запустить проверку» | Обработка |
| 3 | **Э4 Обработка** | 4 стадии (OCR ✓, NLP ✓, CV активна, Сопоставление), прогресс 68→100 %, живой лог. Кнопка «Открыть протокол» | Протокол |
| 4 | **Э5 Протокол проверки** | 5 вкладок со счётчиками. Вкладка «Кандидаты» активна. В строке M-047 кнопка «Проверить» | Верификация |
| 5 | **Э6 Верификация кандидата** | Три панели: очередь / доказательства / решение. Подтверждение / Отклонение / Уточнение. Клавиши `1`, `2`, `3`, `←`, `→` | Финализация |
| 6 | **Э7 Финализация** | Сводка, экспорт PDF/DOCX/XML, блок ИАИС, модалка подтверждения | — |
| 7 | **Э8 Гипотезы свободного поиска** | Доступна через левый Sidebar или вкладку «Гипотезы» в Протоколе | — |

Дополнительные состояния (демонстрируются кликом по соответствующим элементам):

- **Э1** — ошибка авторизации: оставить поля пустыми и нажать «Войти».
- **Э3** — 5 ошибок загрузки: панель «Проверка ошибок загрузки» в правой колонке, кнопка «Показать все пять ошибок».
- **Э6** — CLARIFICATION_REQUIRED: выбрать кандидата M-122 (конфликт редакций РД Ред. 1 и Ред. 2).
- **Э6** — составной кандидат: выбрать M-118 (6 ветвей местных отсосов).
- **Э6** — «Все кандидаты обработаны»: обработать все 14 кандидатов очереди.

---

## Архитектура проекта

```
.
├── index.html
├── package.json
├── vite.config.ts
├── tsconfig.json
├── README.md
└── src/
    ├── App.tsx                      # Стейт-роутер: ScreenId + NavState
    ├── main.tsx                     # Точка входа, обёртка ToastProvider
    ├── components/
    │   ├── Button.tsx               # primary / secondary / ghost / danger
    │   ├── EmptyState.tsx           # 6 предустановок пустых состояний
    │   ├── EvidencePanel.tsx        # Панель чертежа с bbox-оверлеем в %
    │   ├── PageHeader.tsx           # Хлебные крошки + заголовок + слот действий
    │   ├── PriorityIndicator.tsx    # Три засечки, графит #475569
    │   ├── Sidebar.tsx              # Свёрнутое меню с иконками Lucide
    │   ├── Skeleton.tsx             # Table / Queue / EvidencePanel / Card / Text
    │   ├── StageBadge.tsx           # Бейджи стадий ПД / РД / ИД
    │   ├── StatusBadge.tsx          # 8 статусов находок
    │   └── Toast.tsx                # ToastProvider + useToast (info/success/warning/error)
    ├── mocks/
    │   └── data.ts                  # Единственный источник демо-данных
    ├── screens/
    │   ├── LoginScreen.tsx          # Э1
    │   ├── DashboardScreen.tsx      # Э2
    │   ├── UploadScreen.tsx         # Э3
    │   ├── ProcessingScreen.tsx     # Э4
    │   ├── ProtocolScreen.tsx       # Э5
    │   ├── VerificationScreen.tsx   # Э6 (все 7 состояний)
    │   ├── FinalizationScreen.tsx   # Э7
    │   └── HypothesesScreen.tsx     # Э8
    ├── styles/
    │   └── global.css               # @import "tailwindcss" + Inter / JetBrains Mono
    └── types/
        └── index.ts                 # FindingStatus, ProcessStatus, DocStage, Finding, …
```

---

## Карта переходов между экранами

Стейт-роутинг реализован в `src/App.tsx` без внешних библиотек — через `useState<NavState>` и колбэк `onNavigate`. Схема переходов:

```
                                 ┌──────────────────────┐
                                 │  Э1 · LoginScreen    │
                                 │  (auth → main)       │
                                 └──────────┬───────────┘
                                            │ onLogin
                                            ▼
                                 ┌──────────────────────┐
                                 │  Э2 · DashboardScreen│◄──────────────┐
                                 │  таблица объектов    │               │
                                 └──────────┬───────────┘               │
                                            │ onOpenObject            │
                                            ▼                         │
                                 ┌──────────────────────┐               │
                                 │  Э3 · UploadScreen   │───────────────┘
                                 │  3 Dropzone          │  onBack
                                 └──────────┬───────────┘
                                            │ onRunCheck
                                            ▼
                                 ┌──────────────────────┐
                                 │  Э4 · ProcessingScreen│
                                 │  4 стадии, лог        │
                                 └──────────┬───────────┘
                                            │ onComplete
                                            ▼
                      onOpenHypotheses  ┌──────────────────────┐
                ┌───────────────────────│  Э5 · ProtocolScreen │
                │                       │  5 вкладок           │
                ▼                       └──────────┬───────────┘
      ┌──────────────────────┐                     │ onOpenVerification
      │  Э8 · HypothesesScreen│                    ▼
      │  карточки гипотез     │         ┌──────────────────────┐
      └──────────┬───────────┘         │  Э6 · Verification    │
                 │ onPromote           │  3 панели, 7 состояний│
                 └────────────────────►│  CLARIFICATION /      │
                                       │  composite / queue-end│
                                       └──────────┬───────────┘
                                                  │ onFinish
                                                  ▼
                                       ┌──────────────────────┐
                                       │  Э7 · Finalization    │
                                       │  экспорт + ИАИС       │
                                       └──────────────────────┘
```

Все переходы — только через `onNavigate({ screen, objectId?, protocolId? })`, передаваемый в экраны как props. Никаких URL-хэшей, никаких History API — чистая реактивная машина состояний.

---

## Ключевые правила ТЗ, зашитые в код

| Правило | Где реализовано |
|---|---|
| ИИ не выносит вердикт — только кандидатов | `statusLabels`, колонка «Кандидатов» в Дашборде |
| Красный `#B42318` — только для CONFIRMED_VIOLATION и кнопки «Подтвердить» | `StatusBadge`, `Button variant="danger"`, `EvidencePanel accent="actual"` |
| Приоритет HIGH/MEDIUM/LOW — графит `#475569`, три засечки | `PriorityIndicator` |
| tabular-nums глобально | `font-variant-numeric: tabular-nums` в `body` |
| JetBrains Mono для шифров и SHA-256 | класс `.mono` |
| bbox-оверлей в процентах от подложки | `EvidencePanel` — `left/top/width/height = x * 100%` |
| Решение по кандидату в 2–3 клика, без модалок | `VerificationScreen`, хоткеи `1/2/3` |
| Единственная модалка — финализация | `FinalizationScreen` |
| 5 ошибок загрузки с точным текстом из ТЗ | `UploadScreen`, `useToast` |
| Гипотезы нельзя подтвердить напрямую | `HypothesesScreen` — только «Привязать доказательства» |

---

## Работа с данными

Все демо-данные вынесены в `src/mocks/data.ts`. Компоненты получают их через props или через импорт из `mocks`. **Никакие тексты не хардкодятся в JSX**.

Что лежит в `mocks/data.ts`:

- `objects` — 8 объектов Мосгосстройнадзора (Алтуфьевское шоссе, Полярная, Октябрьская, Лосевская, УНДМС).
- `findings` — 17 находок: 12 кандидатов (M-002, M-047, M-055, M-118, M-122, …), 3 подтверждённых, 5 гипотез. У M-118 — `composite.atoms`, у M-122 — `clarificationConflict.revisions`.
- `uploadedFiles` — 12 файлов ПД + 5 файлов РД с реальными шифрами (АНО/150321/1-П-АР, АНО/150321/1-РД-ОВ1, …) и сгенерированными SHA-256.
- `protocol` — протокол 2025-0147 со сводкой (132 проверено, 14 кандидатов, 3 подтверждено, 96 без расхождений, 12 без доказательств, 7 неприменимо).
- Словари: `statusLabels`, `processStatusLabels`, `approvalLabels`, `reasonLabels`, `detectionLabels`, `uploadErrors`.

Для интеграции с реальным API замените содержимое этих массивов на результат fetch — интерфейсы типов в `src/types/index.ts` совпадают с контрактом бэкенда.

---

## Доступность

- Все иконочные кнопки имеют `aria-label`.
- Оверлеи на чертежах помечены `aria-label`.
- Тост-контейнер — `aria-live="polite"`, ошибки — `role="alert"`.
- Модалка финализации — `role="dialog"`, `aria-modal="true"`, `aria-labelledby`.
- Видимый фокус — `outline: 2px solid #1B4E9B` на всех интерактивных элементах.
- Контраст текста и фонов проверен на ≥ 4.5:1 (янтарный `#B54708` на `#FFFAEB`, голубой `#026AA2` на `#F0F9FF`).

---

## Приёмка по ТЗ — чек-лист

- [x] Все 8 экранов отрисованы в разрешении 1440×900
- [x] Все 8 статусов находок присутствуют и оформлены по разделу 4.2
- [x] Красный применён только к CONFIRMED_VIOLATION и evidence/actual
- [x] Приоритет HIGH/MEDIUM/LOW не окрашен в цвета статусов
- [x] Отрисованы 7 состояний экрана верификации
- [x] Отрисованы 5 сценариев ошибок загрузки
- [x] Пустые состояния у дашборда, протокола и очереди кандидатов
- [x] Никакой текст в интерфейсе не приписывает вынесение нарушений системе
- [x] Решение по кандидату без модального окна
- [x] Подтверждение в 2 клика, отклонение в 3
- [x] Ожидаемое, фактическое и оба чертежа видны одновременно
- [x] Протокол содержит ровно 5 вкладок
- [x] Комплектность и кандидаты не смешаны в одной таблице
- [x] Гипотезы нельзя подтвердить как нарушение напрямую
- [x] Координаты подсветки нормализованы в [0;1] и позиционируются в %
- [x] Нет сетевых вызовов и внешних URL на изображения